{
  "routes_allowed": [
    {
      "method": "GET",
      "pattern": "^/containers/json$"
    },
    {
      "method": "*",
      "pattern": "^/containers/(.*mariadb.*)/(json|start|stop|exec)$"
    },
    {
      "method": "*",
      "pattern": "^/exec/([a-f0-9].+)/(start|json|resize)$"
    }
  ]
}
